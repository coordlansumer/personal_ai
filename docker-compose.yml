services:
  ai-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-ai-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
